---
title: "isEmail"
section: "Utilities"
description: "Validate if a given string is a valid email address using the NFSFU234FormValidation Library"
relatedLinks:
  - title: "isURL"
    description: "Validate if a given string is a valid URL."
    link: "/docs/isURL"
  - title: "isOnline"
    description: "Checks if the browser is currently online."
    link: "/docs/isOnline"
  - title: "isZIP"
    description: "Validate if a given value is a valid ZIP code."
    link: "/docs/isZIP"
  - title: "countString"
    description: "Count the number of characters in a string."
    link: "/docs/countString"
---

<Headings.h4 className="text-white bg-black inline-block px-4 py-2 rounded-full">isEmail</Headings.h4>

<Paragraph className="mt-2 text-gray-400">
The `isEmail` function is used to validate whether a given string is a valid email address.
</Paragraph>

<WorksIn visibleTypes={['html5', 'reactJS', 'nodejs']} />

<ParameterList
  parameters={[
    {
      name: 'email',
      description: 'The string to validate as an email address.',
      type: 'string',
      dataType: 'string',
      defaultValue: 'undefined',
      returnType: 'boolean'
    }
  ]}
/>

<Headings.h5 className="mt-4">Usage</Headings.h5>

<Headings.h6 className="mt-4">Example: Validate Email</Headings.h6>

<CodeBlock>
{`const email = 'example@example.com';
const isValidEmail = nfsfu234FormValidation.isEmail(email);
console.log(isValidEmail); // true`}
</CodeBlock>

<Headings.h6 className="mt-4">Another Example: Validate Email</Headings.h6>

<CodeBlock>
{`const email = 'example@example';
const isValidEmail = nfsfu234FormValidation.isEmail(email);
console.log(isValidEmail); // false`}
</CodeBlock>


<RelatedLinks />

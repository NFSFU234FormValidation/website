---
title: "hashPassword"
section: "Utilities"
description: "Hashes a password using bcrypt."
relatedLinks:
  - title: "generatePassword"
    description: "Generate a random password with optional hashing."
    link: "/docs/generatePassword"
  - title: "passwordMatch"
    description: "Verify if a password matches a hashed password."
    link: "/docs/passwordMatch"
  - title: "togglePasswordVisibility"
    description: "Toggles the visibility of a password input and updates the corresponding visibility icon."
    link: "/docs/togglePasswordVisibility"
---

<Headings.h4 className="text-white bg-black inline-block px-4 py-2 rounded-full">hashPassword</Headings.h4>

<Paragraph className="mt-2 text-gray-400">
The `hashPassword` function hashes a plain text password using `bcrypt` and returns the hashed password as a promise.
</Paragraph>

<WorksIn visibleTypes={['nodejs', 'reactJS', 'html5']} />

<ParameterList
  parameters={[
    {
      name: 'password',
      description: 'The password to be hashed.',
      type: 'string',
      dataType: 'string',
      defaultValue: 'undefined',
      returnType: 'Promise<string>'
    }
  ]}
/>

<Headings.h5 className="mt-4">Usage</Headings.h5>

<Headings.h6 className="mt-4">Example: Hash a Password</Headings.h6>

<CodeBlock>
{`nfsfu234FormValidation.hashPassword('@Password12345')
  .then(hashedPassword => {
    console.log(hashedPassword); // The hashed password
  })
  .catch(error => {
    console.error("Error hashing password: ", error);
  });
`}
</CodeBlock>

<RelatedLinks />

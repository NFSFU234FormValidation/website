---
title: "togglePasswordVisibility"
section: "Utilities"
description: "Toggles the visibility of a password input and updates the corresponding visibility icon."
relatedLinks:
  - title: "generatePassword"
    description: "Generate a random password with optional hashing."
    link: "/docs/generatePassword"
  - title: "hashPassword"
    description: "Hashes a password using bcrypt."
    link: "/docs/hashPassword"
  - title: "togglePasswordVisibilityAll"
    description: "Toggles the visibility of a password input and updates the corresponding visibility icon."
    link: "/docs/togglePasswordVisibilityAll"
---

<Headings.h4 className="text-white bg-black inline-block px-4 py-2 rounded-full">togglePasswordVisibility</Headings.h4>

<Paragraph className="mt-2 text-gray-400">
The `togglePasswordVisibility` function toggles the visibility of a password input field between 'password' and 'text' and updates the corresponding visibility icon.
</Paragraph>

<WorksIn visibleTypes={['reactJS', 'html5']} />

<ParameterList
  parameters={[
    {
      name: 'input',
      description: 'The password input element.',
      type: 'HTMLInputElement',
      dataType: 'HTMLElement',
      defaultValue: 'undefined',
      returnType: 'void'
    },
    {
      name: 'showIcon',
      description: 'Icon for showing the password.',
      type: 'string | HTMLElement | null',
      dataType: 'HTMLElement',
      defaultValue: 'null',
      returnType: 'void'
    },
    {
      name: 'hideIcon',
      description: 'Icon for hiding the password.',
      type: 'string | HTMLElement | null',
      dataType: 'HTMLElement',
      defaultValue: 'null',
      returnType: 'void'
    }
  ]}
/>

<Headings.h5 className="mt-4">Usage</Headings.h5>

<Headings.h6 className="mt-4">Example: Toggle Password Visibility</Headings.h6>

<CodeBlock>
{`const passwordInputField = document.querySelector("input[type=password]");
const showIcon = '🙈';
const hideIcon = '👀';

nfsfu234FormValidation.togglePasswordVisibility(passwordInputField, showIcon, hideIcon);
`}
</CodeBlock>

<RelatedLinks />

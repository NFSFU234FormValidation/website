---
title: "isNumber"
section: "Utilities"
description: "Check if a given value is a number using the NFSFU234FormValidation Library"
relatedLinks:
  - title: "containsOnlyIntegers"
    description: "Checks if a string contains only integer digits."
    link: "/docs/containsOnlyIntegers"
  - title: "isURL"
    description: "Validate if a given string is a valid URL."
    link: "/docs/isURL"
  - title: "isOnline"
    description: "Checks if the browser is currently online."
    link: "/docs/isOnline"
  - title: "isZIP"
    description: "Validate if a given value is a valid ZIP code."
    link: "/docs/isZIP"
---

<Headings.h4 className="text-white bg-black inline-block px-4 py-2 rounded-full">isNumber</Headings.h4>

<Paragraph className="mt-2 text-gray-400">
The `isNumber` function checks if the given value is a number.
</Paragraph>

<WorksIn visibleTypes={['html5', 'reactJS', 'nodejs']} />

<ParameterList
  parameters={[
    {
      name: 'number',
      description: 'The value to check.',
      type: 'string | number',
      dataType: 'mixed',
      defaultValue: 'undefined',
      returnType: 'boolean'
    }
  ]}
/>

<Headings.h5 className="mt-4">Usage</Headings.h5>

<Headings.h6 className="mt-4">Example: Check if Value is a Number</Headings.h6>

<CodeBlock>
{`const value = 123;
const isValidNumber = nfsfu234FormValidation.isNumber(value);
console.log(isValidNumber); // true`}
</CodeBlock>

<Headings.h6 className="mt-4">Example: Check if Value is a Number</Headings.h6>

<CodeBlock>
{`const value = "123";
const isValidNumber = nfsfu234FormValidation.isNumber(value);
console.log(isValidNumber); // true`}
</CodeBlock>


<RelatedLinks />

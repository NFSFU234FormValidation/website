---
title: "togglePasswordVisibilityAll"
section: "Utilities"
description: "Toggles the visibility of password inputs and updates corresponding visibility icons."
relatedLinks:
  - title: "togglePasswordVisibility"
    description: "Toggles the visibility of a password input and updates the corresponding visibility icon."
    link: "/docs/togglePasswordVisibility"
  - title: "generatePassword"
    description: "Generate a random password with optional hashing."
    link: "/docs/generatePassword"
  - title: "hashPassword"
    description: "Hashes a password using bcrypt."
    link: "/docs/hashPassword"
---

<Headings.h4 className="text-white bg-black inline-block px-4 py-2 rounded-full">togglePasswordVisibilityAll</Headings.h4>

<Paragraph className="mt-2 text-gray-400">
The `togglePasswordVisibilityAll` function toggles the visibility of all password inputs within a specified form and updates the corresponding visibility icons.
</Paragraph>

<WorksIn visibleTypes={['html5', 'reactJS']} />

<ParameterList
  parameters={[
    {
      name: 'icons',
      description: 'Object containing icons for showing and hiding the password.',
      type: 'object',
      dataType: 'object',
      defaultValue: '{}',
      returnType: 'void',
      properties: [
        {
          name: 'show',
          description: 'Icon for showing the password.',
          type: 'string | HTMLElement | null',
          defaultValue: 'null',
        },
        {
          name: 'hide',
          description: 'Icon for hiding the password.',
          type: 'string | HTMLElement | null',
          defaultValue: 'null',
        },
      ]
    },
    {
      name: 'uform',
      description: 'The form element or its ID.',
      type: 'string | HTMLFormElement | HTMLDivElement | null',
      dataType: 'HTMLElement',
      defaultValue: 'null',
      returnType: 'void'
    },
    {
      name: 'toggleAll',
      description: 'Flag indicating whether to toggle all password inputs.',
      type: 'boolean',
      dataType: 'boolean',
      defaultValue: 'false',
      returnType: 'void'
    }
  ]}
/>

<Headings.h5 className="mt-4">Usage</Headings.h5>

<Headings.h6 className="mt-4">Example: Toggle Visibility of All Password Inputs</Headings.h6>

<CodeBlock>
{`const icons = { show: '🙈', hide: '👀' };
const form = document.getElementById('myForm');

nfsfu234FormValidation.togglePasswordVisibilityAll(icons, form, true);
`}
</CodeBlock>

<RelatedLinks />

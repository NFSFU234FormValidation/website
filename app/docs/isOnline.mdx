---
title: "isOnline"
section: "Utilities"
description: "Checks if the browser is currently online."
relatedLinks:
  - title: "isURL"
    description: "Validate if a given string is a valid URL."
    link: "/docs/isURL"
  - title: "isNumber"
    description: "Check if a given value is a number."
    link: "/docs/isNumber"
  - title: "isZIP"
    description: "Validate if a given value is a valid ZIP code."
    link: "/docs/isZIP"
  - title: "containsOnlyIntegers"
    description: "Checks if a string contains only integer digits."
    link: "/docs/containsOnlyIntegers"
---

<Headings.h4 className="text-white bg-black inline-block px-4 py-2 rounded-full">isOnline</Headings.h4>

<Paragraph className="mt-2 text-gray-400">
The `isOnline` function checks if the browser is currently online. It returns `true` if the browser reports that it is online, and `false` otherwise. This function is only applicable in a browser environment.
</Paragraph>

<WorksIn visibleTypes={['html5', 'reactJS']} />



<Headings.h5 className="mt-4">Usage</Headings.h5>

<Headings.h6 className="mt-4">Example: Check Browser Online Status</Headings.h6>

<CodeBlock>
{`const onlineStatus = nfsfu234FormValidation.isOnline();
console.log(onlineStatus); // true or false`}
</CodeBlock>


<RelatedLinks />

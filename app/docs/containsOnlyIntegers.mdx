---
title: "containsOnlyIntegers"
section: "Utilities"
description: "Checks if a string contains only integer digits."
relatedLinks:
  - title: "isNumber"
    description: "Check if a given value is a number."
    link: "/docs/isNumber"
---

<Headings.h4 className="text-white bg-black inline-block px-4 py-2 rounded-full">containsOnlyIntegers</Headings.h4>

<Paragraph className="mt-2 text-gray-400">
The `containsOnlyIntegers` function checks if a given string contains only integer digits (0-9). It returns `true` if the string matches the pattern of one or more digits, otherwise `false`.
</Paragraph>

<WorksIn visibleTypes={['html5', 'reactJS', 'nodejs']} />

<ParameterList
  parameters={[
    {
      name: 'str',
      description: 'The string to check for integer digits.',
      type: 'string',
      dataType: 'string',
      defaultValue: 'undefined',
      returnType: 'boolean'
    }
  ]}
/>

<Headings.h5 className="mt-4">Usage</Headings.h5>

<Headings.h6 className="mt-4">Example: Check String for Integers</Headings.h6>

<CodeBlock>
{`const str1 = "12345";
const str2 = "123a45";

console.log(nfsfu234FormValidation.containsOnlyIntegers(str1)); // true
console.log(nfsfu234FormValidation.containsOnlyIntegers(str2)); // false`}
</CodeBlock>

<RelatedLinks />

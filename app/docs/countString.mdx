---
title: "countString"
section: "Utilities"
description: "Count the number of characters in a string using the NFSFU234FormValidation Library"
relatedLinks:
  - title: "isURL"
    description: "Validate if a given string is a valid URL."
    link: "/docs/isURL"
  - title: "isOnline"
    description: "Checks if the browser is currently online."
    link: "/docs/isOnline"
  - title: "isEmail"
    description: "Validate if a given string is a valid email address."
    link: "/docs/isEmail"
  - title: "countString"
    description: "Count the number of characters in a string."
    link: "/docs/countString"
---

<Headings.h4 className="text-white bg-black inline-block px-4 py-2 rounded-full">countString</Headings.h4>

<Paragraph className="mt-2 text-gray-400">
The `countString` function counts the number of characters in a given string.
</Paragraph>

<WorksIn visibleTypes={['html5', 'reactJS', 'nodejs']} />

<ParameterList
  parameters={[
    {
      name: 'str',
      description: 'The string whose characters are to be counted.',
      type: 'string',
      dataType: 'string',
      defaultValue: 'undefined',
      returnType: 'number'
    }
  ]}
/>

<Headings.h5 className="mt-4">Usage</Headings.h5>

<Headings.h6 className="mt-4">Example: Count Characters in a String</Headings.h6>

<CodeBlock>
{`const text = 'Hello World';
const characterCount = nfsfu234FormValidation.countString(text);
console.log(characterCount); // 11`}
</CodeBlock>

<RelatedLinks />

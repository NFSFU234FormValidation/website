---
title: "isZIP"
section: "Utilities"
description: "Validate if a given value is a valid ZIP code using the NFSFU234FormValidation Library"
relatedLinks:
  - title: "isURL"
    description: "Validate if a given string is a valid URL."
    link: "/docs/isURL"
  - title: "isOnline"
    description: "Checks if the browser is currently online."
    link: "/docs/isOnline"
  - title: "isNumber"
    description: "Check if a given value is a number."
    link: "/docs/isNumber"
  - title: "isEmail"
    description: "Validate if a given string is a valid email."
    link: "/docs/isEmail"
---

<Headings.h4 className="text-white bg-black inline-block px-4 py-2 rounded-full">isZIP</Headings.h4>

<Paragraph className="mt-2 text-gray-400">
The `isZIP` function validates whether a given value is a valid ZIP code.
</Paragraph>

<WorksIn visibleTypes={['html5', 'reactJS', 'nodejs']} />

<ParameterList
  parameters={[
    {
      name: 'zipCode',
      description: 'The value to validate as a ZIP code.',
      type: 'string | number',
      dataType: 'mixed',
      defaultValue: 'undefined',
      returnType: 'boolean'
    }
  ]}
/>

<Headings.h5 className="mt-4">Usage</Headings.h5>

<Headings.h6 className="mt-4">Example: Validate ZIP Code</Headings.h6>

<CodeBlock>
{`const zipCode = '12345';
const isValidZIP = nfsfu234FormValidation.isZIP(zipCode);
console.log(isValidZIP); // true`}
</CodeBlock>

<RelatedLinks />

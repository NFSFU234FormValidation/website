---
title: "isURL"
section: "Utilities"
description: "Validate if a given string is a valid URL using the NFSFU234FormValidation Library"
relatedLinks:
  - title: "getPageUrl"
    description: "Retrieves the current page URL from the browser's window object."
    link: "/docs/getPageUrl"
  - title: "AJAX"
    description: "Learn how to set up and use the AJAX function."
    link: "/docs/ajax"
  - title: "isEmail"
    description: "Validate if a given string is a valid email address."
    link: "/docs/isEmail"
---

<Headings.h4 className="text-white bg-black inline-block px-4 py-2 rounded-full">isURL</Headings.h4>

<Paragraph className="mt-2 text-gray-400">
The `isURL` function is used to validate whether a given string is a valid URL.
</Paragraph>

<WorksIn visibleTypes={['html5', 'reactJS', 'nodejs']} />

<ParameterList
  parameters={[
    {
      name: 'url',
      description: 'The string to validate as a URL.',
      type: 'string',
      dataType: 'string',
      defaultValue: 'undefined',
      returnType: 'boolean'
    }
  ]}
/>

<Headings.h5 className="mt-4">Usage</Headings.h5>

<Headings.h6 className="mt-4">Example: Validate URL</Headings.h6>

<CodeBlock>
{`const url = 'https://www.example.com';
const isValidURL = nfsfu234FormValidation.isURL(url);
console.log(isValidURL); // true`}
</CodeBlock>

<RelatedLinks />

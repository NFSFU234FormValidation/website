---
title: "validateAllSelect"
section: "Form Validation"
description: "Validates all select inputs within a form and returns ErrorMessageInterface."
relatedLinks:
  - title: "customErrorMessageInterface"
    description: "Defines the structure of custom error messages for different input types in the NFSFU234FormValidation Library."
    link: "/docs/customErrorMessageInterface"
  - title: "ErrorMessageInterface"
    description: "Defines the structure of the error messages returned by the validation functions in NFSFU234FormValidation Library."
    link: "/docs/errorMessageInterface"
  - title: "displayError"
    description: "Display error messages in various formats using the NFSFU234FormValidation Library"
    link: "/docs/displayError"
  - title: "validateSelect"
    description: "Validates a select input field and returns an ErrorMessageInterface."
    link: "/docs/validateSelect"
---

<Headings.h4 className="text-white bg-black inline-block px-4 py-2 rounded-full">validateAllSelect</Headings.h4>

<Paragraph className="mt-2 text-gray-400">
The `validateAllSelect` function validates all select inputs within a specified form. It returns an array of error messages (a nested <Link href="/docs/errorMessageInterface" className="text-blue-600 font-semibold" >ErrorMessageInterface</Link>) if any select input is invalid, or a single instance of <Link href="/docs/errorMessageInterface" className="text-blue-600 font-semibold" >ErrorMessageInterface</Link> if all fields are valid.
</Paragraph>

<WorksIn visibleTypes={['html5', 'reactJS']} />

<ParameterList
  parameters={[
    {
      name: 'form',
      description: 'The form element or its ID containing select inputs.',
      type: 'HTMLFormElement | HTMLDivElement | string',
      dataType: 'HTMLElement | string',
      defaultValue: 'undefined',
      returnType: 'ErrorMessageInterface[] | boolean'
    },
    {
      name: 'options',
      description: 'Optional settings for validation such as custom error messages.',
      type: 'object',
      dataType: 'object',
      defaultValue: '{}',
      returnType: 'ErrorMessageInterface[] | boolean'
    }
  ]}
/>

<Headings.h5 className="mt-4">Usage</Headings.h5>

<Headings.h6 className="mt-4">Example: Validate All Select Inputs in a Form</Headings.h6>

<CodeBlock>
{`const formElement = document.getElementById('myForm');
const result = validateAllSelect(formElement, {
  message: 'Please select an option from all dropdowns.'
});
console.log(result);
`}
</CodeBlock>

<RelatedLinks />

---
title: "getFormDetails"
section: "APIs"
description: "Retrieve data from form elements using the NFSFU234FormValidation Library"
relatedLinks:
  - title: "AJAX"
    description: "Learn how to set up and use the AJAX function."
    link: "/docs/ajax"
  - title: "Validate"
    description: "Learn how to validate form data."
    link: "/docs/validate"
  - title: "Submit"
    description: "Learn how to submit forms."
    link: "/docs/submit"
---

<Headings.h4 className="text-white bg-black inline-block px-4 py-2 rounded-full">getFormDetails</Headings.h4>

<Paragraph className="mt-2 text-gray-400">
The `getFormDetails` function is used to extract data from input fields, textareas, and select elements within a specified form. The function collects data from elements with the attribute `'data-attr-name'` or `'name'`, and returns an object containing the form's data.
</Paragraph>

<WorksIn visibleTypes={['html5', 'reactJS']} />

<ParameterList
  parameters={[
    {
      name: 'form',
      description: 'The HTML form element or its ID from which to extract data. This can be a string (ID of the form), an HTMLFormElement, or an HTMLDivElement.',
      type: 'HTMLFormElement | HTMLDivElement | string',
      dataType: 'mixed',
      defaultValue: 'undefined',
      returnType: 'Object | boolean | ErrorMessageInterface'
    }
  ]}
/>

<Headings.h5 className="mt-4">Usage</Headings.h5>

<Headings.h6 className="mt-4">Example: Retrieve Form Details</Headings.h6>

<CodeBlock>

{`const formDetails = nfsfu234FormValidation.getFormDetails('myFormId');
console.log("Form Details: ", formDetails);
`}
</CodeBlock>

<Headings.h6 className="mt-4">Example Form HTML</Headings.h6>

<CodeBlock>
{`<form id="myFormId">
  <input type="text" name="fieldName1" value="value1" data-attr-name="fieldName1" />
  <input type="text" name="fieldName2" value="value2" data-attr-name="fieldName2" />
  <input type="checkbox" name="checkboxName" checked data-attr-name="checkboxName" />
  <input type="radio" name="radioName" data-attr-name="radioName" />
  <textarea name="textareaName" data-attr-name="textareaName">Sample text</textarea>
  <select name="selectName" data-attr-name="selectName">
    <option value="option1" selected>Option 1</option>
    <option value="option2">Option 2</option>
  </select>
</form>`}
</CodeBlock>

<Headings.h6 className="mt-4">JavaScript to Retrieve Form Details</Headings.h6>

<CodeBlock>
{`const formDetails = nfsfu234FormValidation.getFormDetails('myFormId');
console.log("Form Details: ", formDetails);`}
</CodeBlock>

<Headings.h6 className="mt-4">Sample Output</Headings.h6>

<CodeBlock>
{`{
  "fieldName1": "value1",
  "fieldName2": "value2",
  "checkboxName": true,
  "radioName": false,
  "textareaName": "Sample text",
  "selectName": "option1"
}`}
</CodeBlock>

<div class="information-badge bg-black text-white p-2 my-3 text-[12px] rounded-sm">

  <b class="text-lg">Note</b>: If you call the function without having either the <b>`'name'`</b> or the <b>`'data-attr-name'`</b> attribute, then the key values will be index number of arrays. Find the example below:

</div>

<Headings.h6 className="mt-4">Sample Output Without adding the <b>`'name'`</b> or the <b>`'data-attr-name'`</b> HTML Attribute</Headings.h6>

<CodeBlock>
{`{
  0: "value1",
  1: "value2",
  2: true,
  3: false,
  4: "Sample text",
  5: "option1"
}`}
</CodeBlock>

<RelatedLinks />

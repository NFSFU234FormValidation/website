---
title: "displayError"
section: "APIs"
description: "Display error messages in various formats using the NFSFU234FormValidation Library"
relatedLinks:
  - title: "Validate"
    description: "Learn how to validate form data with the NFSFU234FormValidation Library."
    link: "/docs/validate"
  - title: "Submit"
    description: "Learn how to submit forms using the NFSFU234FormValidation Library."
    link: "/docs/submit"
  - title: "AJAX"
    description: "Learn how to set up and use the AJAX function of the NFSFU234FormValidation Library."
    link: "/docs/ajax"
---

<Headings.h4 className="text-white bg-black inline-block px-4 py-2 rounded-full">displayError</Headings.h4>

<Paragraph className="mt-2 text-gray-400">
The `displayError` function is used to display error messages in either an `inline` or `modal` format. It allows specifying the duration for which the error message should be visible and associates the error message with a specific HTML element.
</Paragraph>

<WorksIn visibleTypes={['html5', 'reactJS']} />

<ParameterList
  parameters={[
    {
      name: 'details',
      description: 'The details of the error message to be displayed.',
      type: 'Object',
      dataType: 'object',
      defaultValue: 'undefined'
    },
    {
      name: 'details.type',
      description: 'The type of error display (either "inline" or "modal").',
      type: 'string',
      dataType: 'string',
      defaultValue: 'undefined'
    },
    {
      name: 'details.message',
      description: 'The error message to be displayed.',
      type: 'string',
      dataType: 'string',
      defaultValue: 'undefined'
    },
    {
      name: 'details.duration',
      description: 'The duration (in milliseconds) for which the error message should be visible.',
      type: 'number',
      dataType: 'number',
      defaultValue: 'undefined'
    },
    {
      name: 'details.element',
      description: 'The HTML element to which the error message is associated.',
      type: 'HTMLInputElement | HTMLSelectElement | HTMLTextAreaElement',
      dataType: 'HTMLElement',
      defaultValue: 'undefined'
    },
    {
      name: 'details.success',
      description: 'A flag indicating whether the operation was successful. Default is false.',
      type: 'boolean',
      dataType: 'boolean',
      defaultValue: 'false'
    }
  ]}
/>

<Headings.h5 className="mt-4">Usage</Headings.h5>

<Headings.h6 className="mt-4">Example: Display Error Message</Headings.h6>

<CodeBlock>

{`const errorDetails = {
    type: 'modal', // or inline
    message: 'Hello World👋',
    duration: 5000,
    element: document.getElementById('myFormId'), // could also be an HTML input element
    success: false // or true
};

nfsfu234FormValidation.displayError(errorDetails);
`}
</CodeBlock>

<Headings.h6 className="mt-4">Example Form HTML</Headings.h6>

<CodeBlock>
{`<form id="myFormId">
  <input type="text" name="fieldName1" value="value1" data-attr-name="fieldName1" />
  <input type="text" name="fieldName2" value="value2" data-attr-name="fieldName2" />
  <button type="submit">Submit</button>
</form>`}
</CodeBlock>

<Headings.h6 className="mt-4">JavaScript to Display Error Message</Headings.h6>

<CodeBlock>
{`const errorDetails = {
    type: 'modal',
    message: 'Hello World👋',
    duration: 5000,
    element: document.getElementById('myFormId'),
    success: false
};

nfsfu234FormValidation.displayError(errorDetails);
`}
</CodeBlock>

<Headings.h6 className="mt-4">Expected Output</Headings.h6>

<Paragraph className="mt-2 text-gray-400">
An error message "Hello World👋" will be displayed as a modal for 5000 milliseconds (5 seconds) associated with the specified form element.
</Paragraph>

<RelatedLinks />
